ReaderThread (он один) читает данные из файла в буферы и записывает эти буферы в
очередь buffer_queue.


DecoderThread (их много, в соответствии с числом заданных потоков) извлекает буферы из очереди и анализирует. Записывает события в event_queue.

ProcessorThread (он один) считывает события из event_queue и записывает в all_events.
